pluginManagement {
    plugins {
        id 'io.quarkus' version "${quarkusPlatformVersion}"
    }
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'WeatherService'
